<h2>Games</h2>

<% if @games.any? %>
  <ul>
    <% @games.each do |game| %>
      <li>
        <%= link_to game.name, game_path(game) %>
        [<%= link_to 'Edit', edit_game_path(game) %>]
      </li>
    <% end %>
  </ul>
<% else %>
  <p>No games!</p>
<% end %>

<%= link_to 'Create', new_game_path %> a new game.
